---
import '../styles/global.css';
// Self-hosted, sitewide fonts only — Fraunces (display) and IBM Plex Sans
// (body/UI). Caveat is bookshelf-only and is imported from
// components/bookshelf/Bookshelf.astro instead, never here.
import '@fontsource-variable/fraunces/full.css';
import '@fontsource/ibm-plex-sans/latin.css';
import Header from '../components/Header.astro';
import Footer from '../components/Footer.astro';

interface Props {
  title?: string;
  description?: string;
}

const {
  title = 'Ashwin Aggarwal',
  description = 'Personal site and reading room.',
} = Astro.props;
---

<!doctype html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <link rel="icon" type="image/svg+xml" href="/favicon.svg" />
    <meta name="description" content={description} />
    <title>{title}</title>
  </head>
  <body>
    <Header />
    <main>
      <slot />
    </main>
    <Footer />
    <script>
      import { initTypewriter } from '../scripts/typewriter.js';

      initTypewriter();
    </script>
  </body>
</html>
